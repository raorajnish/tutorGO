You’re building a **large multi-tenant ERP (SmartClass ERP)**, so the Django structure must be **modular**, otherwise the project will become impossible to maintain. Below is a **production-grade Django architecture** used in SaaS systems.

I’ll show:

1️⃣ **Django Apps Architecture (12 apps)**
2️⃣ **Folder Structure**
3️⃣ **Tenant Middleware + Base Model**
4️⃣ **Permission Decorators & Managers**
5️⃣ **Models per App (core fields)**
6️⃣ **What each user role can do in each app**

---

# 1️⃣ SmartClass ERP – Django Apps Architecture

```
smartclass/
│
├── core                # base models, tenant mixins, utilities
├── accounts            # users, roles, permissions
├── institutes          # institute/tenant management
├── academics           # courses, batches, subjects
├── students            # student profiles
├── admissions          # enquiries, admissions
├── attendance          # lectures, attendance
├── devices             # biometric devices
├── finance             # ledger, categories
├── fees                # fee structure & installments
├── faculty             # faculty profile + salary
├── communication       # sms/whatsapp templates
├── audit               # audit logs
```

This architecture keeps each module **independent and scalable**.

---

# 2️⃣ Django Folder Structure (Production)

```
smartclass/
│
├── manage.py
│
├── config/
│   ├── settings.py
│   ├── urls.py
│   ├── asgi.py
│   └── wsgi.py
│
├── core/
│   ├── models.py
│   ├── middleware.py
│   ├── managers.py
│   ├── permissions.py
│   └── services.py
│
├── accounts/
│   ├── models.py
│   ├── views.py
│   ├── serializers.py
│   ├── services.py
│   └── permissions.py
│
├── institutes/
├── academics/
├── students/
├── admissions/
├── attendance/
├── devices/
├── finance/
├── fees/
├── faculty/
├── communication/
├── audit/
```

---

# 3️⃣ Core App (Tenant Base)

This ensures **every table belongs to an institute automatically**.

## core/models.py

```python
from django.db import models

class TimeStampedModel(models.Model):
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        abstract = True


class InstituteBaseModel(TimeStampedModel):
    institute = models.ForeignKey(
        "institutes.Institute",
        on_delete=models.CASCADE,
        related_name="%(class)s_objects"
    )

    class Meta:
        abstract = True
```

---

# 4️⃣ Tenant Middleware

## core/middleware.py

```python
class InstituteMiddleware:

    def __init__(self, get_response):
        self.get_response = get_response

    def __call__(self, request):

        if request.user.is_authenticated:
            request.institute = request.user.institute
        else:
            request.institute = None

        return self.get_response(request)
```

Add to `settings.py`

```python
MIDDLEWARE = [
    ...
    "core.middleware.InstituteMiddleware",
]
```

---

# 5️⃣ Query Manager for Tenant Filtering

## core/managers.py

```python
from django.db import models

class TenantQuerySet(models.QuerySet):

    def for_institute(self, institute):
        return self.filter(institute=institute)


class TenantManager(models.Manager):

    def get_queryset(self):
        return TenantQuerySet(self.model, using=self._db)

    def for_institute(self, institute):
        return self.get_queryset().for_institute(institute)
```

---

# 6️⃣ Permission Decorator

## core/permissions.py

```python
from django.core.exceptions import PermissionDenied

def role_required(allowed_roles):

    def decorator(view_func):

        def wrapper(request, *args, **kwargs):

            if request.user.role not in allowed_roles:
                raise PermissionDenied("Permission denied")

            return view_func(request, *args, **kwargs)

        return wrapper

    return decorator
```

Example:

```
@role_required(["admin","owner"])
def create_student():
```

---

# 7️⃣ Institutes App

Handles **multi-tenant SaaS structure**

## institutes/models.py

```python
from django.db import models

class Institute(models.Model):

    code = models.CharField(max_length=20, unique=True)
    name = models.CharField(max_length=255)

    owner_name = models.CharField(max_length=150)

    email = models.EmailField()
    phone = models.CharField(max_length=20)

    address = models.TextField()
    city = models.CharField(max_length=100)

    subscription_plan = models.CharField(max_length=50)

    is_active = models.BooleanField(default=True)

    created_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return self.name
```

---

# 8️⃣ Accounts App

Handles **users, roles, permissions**

## accounts/models.py

```python
from django.contrib.auth.models import AbstractUser
from django.db import models


class User(AbstractUser):

    ROLE_CHOICES = [
        ("owner", "Owner"),
        ("admin", "Admin"),
        ("faculty", "Faculty"),
        ("reception", "Reception"),
        ("student", "Student"),
    ]

    institute = models.ForeignKey(
        "institutes.Institute",
        on_delete=models.CASCADE,
        null=True,
        blank=True
    )

    role = models.CharField(max_length=20, choices=ROLE_CHOICES)

    phone = models.CharField(max_length=20)

    def __str__(self):
        return self.username
```

---

# 9️⃣ Academics App

## academics/models.py

```python
from django.db import models
from core.models import InstituteBaseModel


class Course(InstituteBaseModel):

    name = models.CharField(max_length=200)

    duration_months = models.IntegerField()

    description = models.TextField(blank=True)


class Subject(InstituteBaseModel):

    name = models.CharField(max_length=200)

    code = models.CharField(max_length=50)


class Batch(InstituteBaseModel):

    course = models.ForeignKey(Course, on_delete=models.CASCADE)

    name = models.CharField(max_length=200)

    start_date = models.DateField()

    end_date = models.DateField(null=True)
```

---

# 🔟 Students App

## students/models.py

```python
from django.db import models
from core.models import InstituteBaseModel


class Student(InstituteBaseModel):

    student_code = models.CharField(max_length=30)

    name = models.CharField(max_length=200)

    phone = models.CharField(max_length=20)

    parent_phone = models.CharField(max_length=20)

    dob = models.DateField(null=True)

    admission_date = models.DateField()

    is_active = models.BooleanField(default=True)


class StudentBatch(InstituteBaseModel):

    student = models.ForeignKey(Student, on_delete=models.CASCADE)

    batch = models.ForeignKey(
        "academics.Batch",
        on_delete=models.CASCADE
    )

    joined_at = models.DateField()

    left_at = models.DateField(null=True)
```

---

# 11️⃣ Attendance App

## attendance/models.py

```python
from django.db import models
from core.models import InstituteBaseModel


class Lecture(InstituteBaseModel):

    batch = models.ForeignKey(
        "academics.Batch",
        on_delete=models.CASCADE
    )

    subject = models.ForeignKey(
        "academics.Subject",
        on_delete=models.CASCADE
    )

    faculty = models.ForeignKey(
        "faculty.Faculty",
        on_delete=models.CASCADE
    )

    date = models.DateField()

    start_time = models.TimeField()

    end_time = models.TimeField()


class Attendance(InstituteBaseModel):

    lecture = models.ForeignKey(Lecture, on_delete=models.CASCADE)

    student = models.ForeignKey(
        "students.Student",
        on_delete=models.CASCADE
    )

    status = models.CharField(max_length=20)

    method = models.CharField(max_length=20)

    timestamp = models.DateTimeField(auto_now_add=True)
```

---

# 12️⃣ Devices App

## devices/models.py

```python
from django.db import models
from core.models import InstituteBaseModel


class Device(InstituteBaseModel):

    device_name = models.CharField(max_length=200)

    device_key = models.CharField(max_length=200)

    device_type = models.CharField(max_length=50)

    is_active = models.BooleanField(default=True)

    last_seen = models.DateTimeField(null=True)


class DeviceScanLog(InstituteBaseModel):

    device = models.ForeignKey(Device, on_delete=models.CASCADE)

    student = models.ForeignKey(
        "students.Student",
        on_delete=models.SET_NULL,
        null=True
    )

    fingerprint_id = models.IntegerField()

    scan_time = models.DateTimeField()

    status = models.CharField(max_length=20)
```

---

# 13️⃣ Finance App

## finance/models.py

```python
from django.db import models
from core.models import InstituteBaseModel


class FinanceCategory(InstituteBaseModel):

    name = models.CharField(max_length=200)

    is_income = models.BooleanField()


class FinanceEntry(InstituteBaseModel):

    category = models.ForeignKey(
        FinanceCategory,
        on_delete=models.CASCADE
    )

    amount = models.DecimalField(max_digits=10, decimal_places=2)

    entry_type = models.CharField(max_length=20)

    description = models.TextField()

    reference_type = models.CharField(max_length=50)

    reference_id = models.BigIntegerField()

    created_by = models.ForeignKey(
        "accounts.User",
        on_delete=models.SET_NULL,
        null=True
    )
```

---

# 14️⃣ Fees App

## fees/models.py

```python
from django.db import models
from core.models import InstituteBaseModel


class StudentFeeAccount(InstituteBaseModel):

    student = models.ForeignKey(
        "students.Student",
        on_delete=models.CASCADE
    )

    course_fee = models.DecimalField(max_digits=10, decimal_places=2)

    discount = models.DecimalField(max_digits=10, decimal_places=2)

    final_fee = models.DecimalField(max_digits=10, decimal_places=2)


class FeeInstallment(InstituteBaseModel):

    account = models.ForeignKey(
        StudentFeeAccount,
        on_delete=models.CASCADE
    )

    installment_no = models.IntegerField()

    due_date = models.DateField()

    amount = models.DecimalField(max_digits=10, decimal_places=2)

    status = models.CharField(max_length=20)


class FeePayment(InstituteBaseModel):

    installment = models.ForeignKey(
        FeeInstallment,
        on_delete=models.CASCADE,
        null=True
    )

    student = models.ForeignKey(
        "students.Student",
        on_delete=models.CASCADE
    )

    amount = models.DecimalField(max_digits=10, decimal_places=2)

    payment_mode = models.CharField(max_length=20)

    payment_date = models.DateField()
```

---

# 15️⃣ Faculty App

## faculty/models.py

```python
from django.db import models
from core.models import InstituteBaseModel


class Faculty(InstituteBaseModel):

    user = models.ForeignKey(
        "accounts.User",
        on_delete=models.CASCADE
    )

    full_name = models.CharField(max_length=200)

    salary_type = models.CharField(max_length=50)

    salary_amount = models.DecimalField(max_digits=10, decimal_places=2)

    joined_at = models.DateField()
```

---

# 16️⃣ Communication App

## communication/models.py

```python
from django.db import models
from core.models import InstituteBaseModel


class MessageTemplate(InstituteBaseModel):

    code = models.CharField(max_length=100)

    text = models.TextField()


class MessageLog(InstituteBaseModel):

    template = models.ForeignKey(
        MessageTemplate,
        on_delete=models.SET_NULL,
        null=True
    )

    to_phone = models.CharField(max_length=20)

    message_text = models.TextField()

    sent_by = models.ForeignKey(
        "accounts.User",
        on_delete=models.SET_NULL,
        null=True
    )

    sent_at = models.DateTimeField(auto_now_add=True)
```

---

# 17️⃣ Audit App

## audit/models.py

```python
from django.db import models
from core.models import InstituteBaseModel


class AuditLog(InstituteBaseModel):

    user = models.ForeignKey(
        "accounts.User",
        on_delete=models.SET_NULL,
        null=True
    )

    action = models.CharField(max_length=200)

    model_name = models.CharField(max_length=200)

    object_id = models.BigIntegerField()

    payload = models.JSONField()

    created_at = models.DateTimeField(auto_now_add=True)
```

---

# 18️⃣ What Each Role Can Do

## OWNER

Full control.

Actions:

* create institute settings
* manage admins
* see all finance reports
* export excel
* enable modules
* see device health
* see analytics dashboard

---

## ADMIN

Actions:

* add students
* assign batches
* manage courses
* manage faculty
* manage attendance
* collect fees
* manage expenses
* send SMS

---

## RECEPTION

Actions:

* handle enquiries
* register students
* record fee payments
* generate receipts
* send reminders

---

## FACULTY

Actions:

* see assigned batches
* take attendance
* view student list
* upload notes
* view lecture schedule

---

## STUDENT

Actions:

* view attendance
* see fees due
* download receipts
* see class schedule
* receive announcements

---

# 19️⃣ If you want, I can also generate

**EXTREMELY useful for you right now:**

1️⃣ **Complete `services.py` layer (business logic)**
2️⃣ **Django REST API structure for ERP**
3️⃣ **React dashboard structure**
4️⃣ **Admin panel optimized for institute owners**
5️⃣ **Automatic student code generator (`SP20-26-001`)**
6️⃣ **Fee reminder cron jobs (Celery + Redis)**
7️⃣ **Attendance biometric API for devices**

If you want, I can also show you the **real production architecture used by companies like Zoho / ERPNext** that will make this system **scale to 10,000 institutes**.


